export type StockProduct = {
    id: string | number
    referencial_id: string
    name: string
    description: string
    stock: number
    status: string
    client_account_id: string
    created_at: string
    updated_at: string


    // sku: string PARA MAS ADELANTE
    // minStock: number
    // price: number
}
