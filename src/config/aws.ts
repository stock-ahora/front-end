//LO SAQUE DENET

/*
export const AWS_REGION = process.env.NEXT_PUBLIC_AWS_REGION || 'us-east-1'

// Cognito (User Pools)
export const AWS_COGNITO = {
    userPoolId: process.env.NEXT_PUBLIC_AWS_COGNITO_USER_POOL_ID || '',
    userPoolClientId: process.env.NEXT_PUBLIC_AWS_COGNITO_APP_CLIENT_ID || '',
    domain: process.env.NEXT_PUBLIC_AWS_COGNITO_DOMAIN || '', // p.ej. https://truestock.auth.us-east-1.amazoncognito.com
    redirectSignIn: process.env.NEXT_PUBLIC_AWS_COGNITO_REDIRECT_SIGNIN || 'http://localhost:3000',
    redirectSignOut: process.env.NEXT_PUBLIC_AWS_COGNITO_REDIRECT_SIGNOUT || 'http://localhost:3000',
    // scopes típicos: 'email', 'openid', 'profile'
    oauthScopes: (process.env.NEXT_PUBLIC_AWS_COGNITO_SCOPES || 'email openid profile').split(' ')
}

// API Gateway (firmado SigV4) — lo dejaremos opcional para más adelante
export const AWS_API = {
    endpoint: process.env.NEXT_PUBLIC_AWS_API_ENDPOINT || '', // p.ej. https://abc123.execute-api.us-east-1.amazonaws.com/prod
}

// S3 (para facturas/adjuntos OCR)
export const AWS_S3 = {
    bucket: process.env.NEXT_PUBLIC_AWS_S3_BUCKET || '',
}
*/
